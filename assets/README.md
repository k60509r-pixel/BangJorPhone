# 網站素材資料夾

## logo.svg（已提供）

由使用者自 Canva 匯出的乾淨去背 LOGO（JorPhone），2026-07-17 提供並套用。`index.html` 頁首已改為載入 `assets/logo.svg`。

若之後要更換 LOGO，直接覆蓋這個檔案即可（檔名保持 `logo.svg`，或改檔名的話要同步修改 `index.html` 裡 `<img class="logo" src="assets/logo.svg">` 的路徑）。
