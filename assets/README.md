# 網站素材資料夾

## banner.jpg（目前使用中）

2026-07-18 由使用者提供的寬版橫幅廣告圖（JorPhone / HP Bekas Berkualitas），`index.html` 與 `product.html` 頁首都已改為載入 `assets/banner.jpg`（原始 PNG 約 1.8MB，已轉存為 JPEG quality 85 壓縮至約 260KB，避免拖慢行動網路載入速度）。

若之後要更換橫幅，直接覆蓋這個檔案即可（檔名保持 `banner.jpg`，或改檔名的話要同步修改 `index.html` 與 `product.html` 裡 `<img class="logo" src="assets/banner.jpg">` 的路徑）。

## logo.svg（目前未使用）

由使用者自 Canva 匯出的乾淨去背 LOGO（JorPhone），2026-07-17 曾套用於 `index.html` 頁首，2026-07-18 已被 `banner.jpg` 取代。檔案保留在這裡，之後若想改回單純 LOGO 樣式可以再用。
